// Функція для отримання теми на сервері
export async function getServerTheme(): Promise<string> {
    // Припустимо, що ми можемо отримати тему з запиту або з іншого джерела.
    // Ви можете адаптувати це під ваші потреби, наприклад, читання з cookies або заголовків.
  
    // Залежно від умов, поверніть відповідний клас теми
    // Це простий приклад для темної теми
    if (someConditionForDarkTheme) {
      return 'dark-theme';
    }
    return ''; // Світла тема
  }
  